import 'package:flutter/material.dart';

class QuestionnaryPage extends StatelessWidget {
  const QuestionnaryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
